// Minimal bootstrap for skelet WebApp
document.addEventListener('DOMContentLoaded', () => {
  const root = document.getElementById('root') || document.body;
  root.innerHTML = '<p>Skeleton WebApp loaded. Connect to Supabase for real data.</p>';
});
